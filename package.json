{
  "name": "military-asset-management-system",
  "version": "1.0.0",
  "description": "Monolithic deployment for Render",
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd ../backend && npm install",
    "start": "cd backend && npm start"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
